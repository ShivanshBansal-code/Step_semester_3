# Step_semester_3

## Date: 11-09-2026

**Today's Work:**
- Solved 5 assignment problems for Session 5 (Topic: `session_5_assignment`) covering array modification by reference, nested loops duplicate checking, single-pass min/max tracking, 2D array row averaging, and auto-draft ranking with `Comparable<Player>` and method overloading:
  1. Fantasy Team Score Multiplier (`FantasyTeamScoreMultiplier.java`)
  2. Duplicate Player Pick Checker (`DuplicatePlayerPickChecker.java`)
  3. Top Performer Tracker (`TopPerformerTracker.java`)
  4. Match Day Grid Analyzer (`MatchDayGridAnalyzer.java`)
  5. Fantasy League Auto-Draft Ranking Engine (`Player.java`, `FantasyLeagueAutoDraftRankingEngine.java`)
- Created clean package directory structure under `src/main/java/session_5_assignment/` with `class_problems/` and `assigment_problems/`.
- Verified compilation and tested all sample test cases successfully.
- Committed and pushed the implementation to branch `feature/session_5`.

**Next Session Plan:**
- Work on Session 6 class and assignment problems.

**Issues Faced:**
- None

## Date: 11-09-2026

**Today's Work:**
- Solved 5 assignment problems for Session 4 (Topic: `session_4_assignment`) covering arrays, prefix/suffix products, Kadane's algorithm, two-pointer 3Sum, prefix sum hash maps, and rotated binary search:
  1. Product of Array Except Self (`ProductOfArrayExceptSelf.java`)
  2. Maximum Subarray (`MaximumSubarray.java`)
  3. 3Sum (`ThreeSum.java`)
  4. Subarray Sum Equals K (`SubarraySumEqualsK.java`)
  5. Find Minimum in Rotated Sorted Array (`FindMinimumInRotatedSortedArray.java`)
- Created clean package directory structure under `src/main/java/session_4_assignment/` with `class_problems/` and `assigment_problems/`.
- Verified compilation and tested all sample test cases successfully.
- Committed and pushed the implementation to branch `feature/session_4`.

**Next Session Plan:**
- Work on Session 5 class and assignment problems.

**Issues Faced:**
- None

## Date: 11-09-2026

**Today's Work:**
- Solved 5 assignment problems for Session 3 (Topic: `session_3_assignment`) covering OOP fundamentals, classes, objects, fields, constructors, and static members:
  1. Simple Book Record Class (`Book.java`)
  2. Two Objects, Two Separate Occupancy Counts (`ExamHall.java`)
  3. Reading Java's Default Field Values (`Employee.java`)
  4. Basic Constructor for a Product (`Product.java`)
  5. Counting Objects With a Static Field (`Order.java`)
- Created clean package directory structure under `src/main/java/session_3_assignment/` with `class_problems/` and `assigment_problems/`.
- Verified compilation and tested all sample test cases successfully.
- Committed and pushed the implementation to branch `feature/session_3`.

**Next Session Plan:**
- Work on Session 4 class and assignment problems.

**Issues Faced:**
- None

## Date: 11-09-2026

**Today's Work:**
- Solved 5 assignment problems for Session 2 (Topic: `session_2_assignment`) covering string manipulation, validation, formatting, and frequency counting:
  1. ATM PIN Length Validator (`AtmPinLengthValidator.java`)
  2. Word Reversal Encoder (`WordReversalEncoder.java`)
  3. Product Inventory CSV Parser (`ProductInventoryCsvParser.java`)
  4. Library ISBN Normalizer & Validator (`LibraryIsbnNormalizerValidator.java`)
  5. Stop-Word-Filtered Word Frequency Report (`StopWordFilteredWordFrequencyReport.java`)
- Created clean package directory structure under `src/main/java/session_2_assignment/` with `class_problems/` and `assigment_problems/`.
- Verified compilation and tested all sample test cases successfully.
- Committed and pushed the implementation to branch `feature/session_2`.

**Next Session Plan:**
- Work on Session 3 class and assignment problems.

**Issues Faced:**
- None

## Date: 11-09-2026

**Today's Work:**
- Solved 5 assignment problems for Session 1 (Topic: `session_1_assignment`) covering arrays, loops, and string operations:
  1. The Exam Hall Seat Duplication Checker (`ExamHallSeatDuplicationChecker.java`)
  2. The Typing Speed Test Accuracy Checker (`TypingSpeedTestAccuracyChecker.java`)
  3. The Traffic Signal Streak Analyzer (`TrafficSignalStreakAnalyzer.java`)
  4. The Warehouse Inventory Balancer (`WarehouseInventoryBalancer.java`)
  5. The Movie Review Word Length Profiler (`MovieReviewWordLengthProfiler.java`)
- Created clean package directory structure under `src/main/java/session_1_assignment/` with `class_problems/` and `assigment_problems/`.
- Verified compilation and tested all sample test cases successfully.
- Committed and pushed the implementation to branch `feature/session_1`.

**Next Session Plan:**
- Work on Session 2 class and assignment problems.

**Issues Faced:**
- None
